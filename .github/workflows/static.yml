name: Legacy static deploy disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy static workflow disabled. Use pages.yml for Vite React deployment."
